langchain
langgraph
langchain-openai
langchain-mcp-adapters
# Pin mcp to 1.x: mcp 2.0.0 removed RequestContext from mcp.shared.context,
# which langchain-mcp-adapters imports. Remove the upper bound once the adapter
# ships a release that supports mcp 2.x.
mcp>=1.24.0,<2
openai
requests
